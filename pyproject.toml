[project]
name = "tenma-ps"
version = "26.0.2"
description = "python package for controlling tenma power supply"
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "auto-py-utils", email = "chaitu.ycr@gmail.com" }]
keywords = ["python", "tenma", "power", "supply", "application"]
classifiers = [
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "License :: OSI Approved :: MIT License",
    "Operating System :: Microsoft :: Windows"
]
dependencies = [
    "tenma-serial",
    "customtkinter",
    "fastapi[all]",
]

[dependency-groups]
dev = [
    "pyinstaller",
    "ruff",
    "pytest",
    "pytest-html",
    "pytest-sugar",
    "pytest-clarity",
    "pytest-cov",
    "pytest-watch",
    "mkdocs-puml",
    "mkdocs-exclude",
    "mkdocs-material<10",
    "mkdocs-jupyter",
    "mkdocstrings-python",
    "mkdocs-macros-plugin",
    "mkdocs-include-markdown-plugin",
    "pymdown-extensions",
]

[project.urls]
homepage = "https://github.com/auto-py-utils/tenma-ps"
repository = "https://github.com/auto-py-utils/tenma-ps"
documentation = "https://auto-py-utils.github.io/tenma-ps"

[project.scripts]
tenma-ps-gui = "tenma_ps.ps_gui:main"
tenma-ps-api = "tenma_ps.ps_fastapi:main"

[tool.uv]
package = true

[tool.ruff]
line-length = 120

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
